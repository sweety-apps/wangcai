<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.miidi.sample.banner"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="4" />
    
    <!-- 连接网络权限INTERNET (必须)-->
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- 用于下载前检查网络状态 (必须)-->	
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  
    <!-- 往SDCard写入数据权限(必须) -->  
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- 获取设备相关信息(必须) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />	
	<!-- 获取mac地址 ，支持一些Pad设备不能获取imei的bug(必须) -->
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
	
	<!-- 获取地理位置信息，用于帮助精确统计开发者的程序发布之后的用户分布情况 (可选)-->
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> 
	
	<!-- 支持Push短信广告模式(可选) -->
	<uses-permission android:name="android.permission.WRITE_SMS" />
	<!-- 支持Push短信广告模式(可选) -->
	<uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".MiidiSample_BannerActivity"
            android:label="@string/app_name" 
            android:configChanges="orientation|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
      </activity>      
            
            
        <!-- 上报发布推广应用的电子市场渠道号，请参考帮助文档  (可选)-->
      	<meta-data android:name="miidi_channelid" android:value="000000" />
      	
        <!-- 显示应用列表和详细信息(必须) -->
        <activity android:name="net.miidi.ad.banner.AdBannerActivity" android:configChanges="keyboardHidden|orientation" />        
            
            
        <!-- sample使用，不需要加入-->
        <activity android:name=".BannerXmlActivity" />
        <!-- sample使用，不需要加入-->
	    <activity android:name=".BannerCodeActivity" />
	    <!-- sample使用，不需要加入-->
	    <activity android:name=".BannerSuspendActivity" />
        
    </application>

</manifest>